{
  "name": "@admin/utils",
  "main": "index.js",
  "description": "A package to share common helpers/types/abstraction within the app",
  "version": "0.0.1",
  "dependencies": {},
  "peerDependencies": {
    "@hapi/boom": "^7.4.2",
    "@hapi/joi": "^15.0.3",
    "koa-router": "^7.4.0"
  },
  "scripts": {
    "build": "npm run clean:dist && yarn build:ts",
    "postinstall": "npm run build",
    "build:ts": "tsc",
    "clean:dist": "rm -rf ./dist"
  }
}
